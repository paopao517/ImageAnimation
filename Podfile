# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageAnimateMemory' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageAnimateMemory
  pod 'Masonry', '~> 1.1.0'

  target 'ImageAnimateMemoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageAnimateMemoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
